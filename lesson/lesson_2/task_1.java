package lesson_2;
// На вход принимаем 3 числа от пользователя, 
// нужно найти максимальное, минимальное и лишнее


  Scanner in  = new Scanner(System.in)
  int x = in.nextInt();
  int y = in.nextInt();    
  int z = in.nextInt();
  int min = x;
  int max= x;
  int another = x;
  if(min<x){
      min = x;
  if(min<y){
      min = y;
  if(min<z){
      min = z;
  if(min<y && max>y)
  }
